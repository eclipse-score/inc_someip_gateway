diff -ruN a/score/itf/plugins/qemu/config.py b/score/itf/plugins/qemu/config.py
--- a/score/itf/plugins/qemu/config.py	2026-05-19 10:14:34.513308102 +0000
+++ b/score/itf/plugins/qemu/config.py	2026-05-19 10:15:05.489613604 +0000
@@ -73,6 +73,7 @@
 import json
 import logging
 import ipaddress
+from typing import Literal
 
 from pydantic import BaseModel, ConfigDict, Field, ValidationError, field_validator
 
@@ -117,6 +118,7 @@
     qemu_num_cores: int = Field(ge=1)
     qemu_ram_size: str = Field(pattern=_RAM_SIZE_PATTERN)
     port_forwarding: list[PortForwarding] = Field(default_factory=list)
+    boot_mode: Literal["kernel", "disk"] = Field(default="kernel")
 
 
 def load_configuration(config_file: str) -> QemuConfigModel:
diff -ruN a/score/itf/plugins/qemu/__init__.py b/score/itf/plugins/qemu/__init__.py
--- a/score/itf/plugins/qemu/__init__.py	2026-05-19 10:14:34.513189222 +0000
+++ b/score/itf/plugins/qemu/__init__.py	2026-05-19 10:15:05.489613604 +0000
@@ -31,6 +31,7 @@
         help="Path to json file with target configurations.",
     )
     parser.addoption("--qemu-image", action="store", help="Path to a QEMU image")
+    parser.addoption("--qemu-seed-iso", action="store", default=None, help="Path to a cloud-init NoCloud seed ISO")
 
 
 @pytest.fixture(scope="session")
@@ -46,6 +47,7 @@
     return Bunch(
         qemu_config=load_configuration(request.config.getoption("qemu_config")),
         qemu_image=request.config.getoption("qemu_image"),
+        qemu_seed_iso=request.config.getoption("qemu_seed_iso"),
     )
 
 
diff -ruN a/score/itf/plugins/qemu/qemu_process.py b/score/itf/plugins/qemu/qemu_process.py
--- a/score/itf/plugins/qemu/qemu_process.py	2026-05-19 10:14:34.513408590 +0000
+++ b/score/itf/plugins/qemu/qemu_process.py	2026-05-19 10:15:05.457335217 +0000
@@ -20,7 +20,7 @@
 
 
 class QemuProcess:
-    def __init__(self, path_to_qemu_image, available_ram, available_cores, network_adapters=[], port_forwarding=[]):
+    def __init__(self, path_to_qemu_image, available_ram, available_cores, network_adapters=[], port_forwarding=[], boot_mode="kernel", seed_iso=None):
         self._path_to_qemu_image = path_to_qemu_image
         self._available_ram = available_ram
         self._available_cores = available_cores
@@ -30,6 +30,8 @@
             self._path_to_qemu_image,
             self._available_ram,
             self._available_cores,
+            boot_mode=boot_mode,
+            seed_iso=seed_iso,
             network_adapters=self._network_adapters,
             port_forwarding=self._port_forwarding,
         )
diff -ruN a/score/itf/plugins/qemu/qemu.py b/score/itf/plugins/qemu/qemu.py
--- a/score/itf/plugins/qemu/qemu.py	2026-05-19 10:14:34.513356902 +0000
+++ b/score/itf/plugins/qemu/qemu.py	2026-05-19 10:15:05.498401893 +0000
@@ -29,6 +29,8 @@
         path_to_image,
         ram="1G",
         cores="2",
+        boot_mode="kernel",
+        seed_iso=None,
         cpu="Cascadelake-Server-v5",
         network_adapters=[],
         port_forwarding=[],
@@ -46,6 +48,8 @@
         self.__path_to_image = path_to_image
         self.__ram = ram
         self.__cores = cores
+        self.__boot_mode = boot_mode
+        self.__seed_iso = seed_iso
         self.__cpu = cpu
         self.__network_adapters = network_adapters
         self.__port_forwarding = port_forwarding
@@ -119,8 +123,9 @@
                 f"{self.__cpu}",  # Specify CPU to emulate
                 "-m",
                 f"{self.__ram}",  # Specify RAM size
-                "-kernel",
-                f"{self.__path_to_image}",  # Specify kernel image
+            ]
+            + self.__image_args()
+            + [
                 "-nographic",  # Disable graphical display (console-only)
                 "-serial",
                 "mon:stdio",  # Redirect serial output to console
@@ -133,6 +138,24 @@
             + self.__port_forwarding_args()
         )
 
+    def __image_args(self):
+        if self.__boot_mode == "disk":
+            args = [
+                "-drive",
+                f"file={self.__path_to_image},format=qcow2,if=virtio",
+            ]
+            if self.__seed_iso:
+                args += [
+                    "-drive",
+                    f"file={self.__seed_iso},format=raw,if=virtio,media=cdrom",
+                ]
+            return args
+        else:
+            return [
+                "-kernel",
+                f"{self.__path_to_image}",
+            ]
+
     def __network_devices_args(self):
         def get_netdev_args(adapter, id):
             return [
diff -ruN a/score/itf/plugins/qemu/qemu_target.py b/score/itf/plugins/qemu/qemu_target.py
--- a/score/itf/plugins/qemu/qemu_target.py	2026-05-19 10:14:34.513453486 +0000
+++ b/score/itf/plugins/qemu/qemu_target.py	2026-05-19 10:15:19.355114838 +0000
@@ -292,6 +292,8 @@
             port_forwarding=test_config.qemu_config.port_forwarding
             if hasattr(test_config.qemu_config, "port_forwarding")
             else [],
+            boot_mode=test_config.qemu_config.boot_mode,
+            seed_iso=getattr(test_config, "qemu_seed_iso", None),
         )
         if test_config.qemu_image
         else nullcontext() as qemu_process
