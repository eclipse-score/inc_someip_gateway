# *******************************************************************************
# Copyright (c) 2024 Contributors to the Eclipse Foundation
#
# See the NOTICE file(s) distributed with this work for additional
# information regarding copyright ownership.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0
#
# SPDX-License-Identifier: Apache-2.0
# *******************************************************************************

# Workflow configuration for S-CORE CI - Bazel Build & Test module
# This workflow runs Bazel build and test when triggered by specific pull request events.

name: Build & Test module (with host toolchain)
on:
  workflow_call:
jobs:
  build_and_test_host:
    runs-on: ubuntu-24.04
    steps:
      - uses: eclipse-score/more-disk-space@v1
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Create Bazel output base directory
        run: |
          sudo mkdir -p /mnt/.bazel
          sudo chown -R $USER:$USER /mnt/.bazel
      - name: Create config indicator file
        run: echo "host" > .bazel_config
      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          output-base: /mnt/.bazel
          # Avoid downloading Bazel every time.
          bazelisk-cache: true
          # Store build cache per workflow.
          disk-cache: ${{ github.workflow }}
          # Share repository cache between workflows.
          repository-cache: |
            - ./MODULE.bazel
            - ./.bazel_config
          # Cache external/
          external-cache: true
          cache-save: ${{ github.event_name == 'push' }}
      - name: Allow linux-sandbox
        uses: ./.github/actions/unblock_user_namespace_for_linux_sandbox
      - run: df -h
      - name: Bazel build targets
        run: |
          bazel build //...
      - run: df -h
        if: always()
      - name: Bazel test targets
        run: |
          bazel test //... --build_tests_only
      - run: df -h
        if: always()

  build_and_int_test_host:
    runs-on: ubuntu-24.04
    steps:
      - uses: eclipse-score/more-disk-space@v1
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Create Bazel output base directory
        run: |
          sudo mkdir -p /mnt/.bazel
          sudo chown -R $USER:$USER /mnt/.bazel
      - name: Create config indicator file
        run: echo "host" > .bazel_config
      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          output-base: /mnt/.bazel
          # Avoid downloading Bazel every time.
          bazelisk-cache: true
          # Store build cache per workflow.
          disk-cache: ${{ github.workflow }}
          # Share repository cache between workflows.
          repository-cache: |
            - ./MODULE.bazel
            - ./.bazel_config
          # Cache external/
          external-cache: true
          cache-save: ${{ github.event_name == 'push' }}
      - name: Install tools needed for ITF tests
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 iputils-ping genisoimage cloud-image-utils
      - name: Allow linux-sandbox
        uses: ./.github/actions/unblock_user_namespace_for_linux_sandbox
      - run: df -h
      - name: Enable KVM group permissons
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Run Linux QEMU integration tests
        run: |
          bazel test //quality/integration_testing/test:test_hello_world --//quality/integration_testing/flags:linux_backend=qemu
      - run: df -h
        if: always()
